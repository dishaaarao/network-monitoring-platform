apiVersion: v1
kind: ConfigMap
metadata:
  name: network-monitoring-config
  namespace: network-monitoring
data:
  APP_ENV: production
  VAULT_ENABLED: "true"
  DB_HOST: network-monitoring-db.example.local
