apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: network-monitoring-app-hpa
  namespace: network-monitoring
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: network-monitoring-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
