apiVersion: v1
kind: Service
metadata:
  name: network-monitoring-app
  namespace: network-monitoring
  labels:
    app: network-monitoring-app
spec:
  type: ClusterIP
  selector:
    app: network-monitoring-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
