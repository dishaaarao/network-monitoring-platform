global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/prometheus-alerts.yml

scrape_configs:
  - job_name: flask-app
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000
